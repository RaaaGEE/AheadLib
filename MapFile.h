


////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Pretreatment
#pragma once
#include <Windows.h>
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////



////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// CMapFile class
class CMapFile
{
public:
	HANDLE m_hFile;		// File handle
	HANDLE m_hMapping;	// Mapping handle
	PVOID m_pvFile;		// Address Mapping
	DWORD m_dwSize;		// File size

public:
	// Constructor, map file is read-only
	WINAPI CMapFile(PCTSTR ptzFileName);

	// Constructor, map files to be read
	WINAPI CMapFile(PCTSTR ptzFileName, DWORD dwSize);

	// Destructor
	WINAPI ~CMapFile();
};
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
